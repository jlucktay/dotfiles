{{- if lookPath "op" -}}
{
  "credentials": {
    "app.terraform.io": {
      "token": "{{ onepasswordRead "op://Private/Terraform Cloud token/credential" "ovotech.1password.com" | trim }}"
    }
  }
}
{{- end }}
